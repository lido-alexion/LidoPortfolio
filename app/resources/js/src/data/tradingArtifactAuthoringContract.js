/**
 * Normative AI Authoring Contract for Trading Artifacts.
 * Single source of MUST/SHOULD rules for the StoX AI Authoring Guide.
 * When behaviour changes, update this contract first, then detailed reference topics.
 */

/** Numbered rule helper */
function R(n, text) {
    return `${n}. ${text}`;
}

/**
 * Full contract body (Markdown). Consumed by AI guide generator and in-app topic.
 */
export const AI_AUTHORING_CONTRACT_MARKDOWN = [
    '# AI Authoring Contract (Normative Rules)',
    '',
    'This Contract is the **constitution** of Trading Artifact authoring.',
    '',
    'Keywords follow RFC 2119: **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, **MAY**.',
    '',
    'Detailed schemas, catalogues, runtime maths, and JSON samples live in later sections. Do not invent behaviour absent from this Contract and those references.',
    '',
    '**Maintenance rule:** Any new authoring or behavioural constraint MUST be added here before it is documented elsewhere in this guide.',
    '',
    '---',
    '',
    '### General Principles',
    '',
    R(1, 'AI MUST generate portable Trading Artifacts (JSON envelopes) consumable by Validate / Import.'),
    R(2, 'AI MUST rely only on functionality documented in this guide.'),
    R(3, 'AI MUST NOT invent undocumented features, operators, indicator ids, parameter names, or optional sections.'),
    R(4, 'AI MUST prefer canonical registry identifiers over aliases.'),
    R(5, 'AI MUST NOT read or depend on application source code to author artifacts.'),
    R(6, 'AI SHOULD minimise unnecessary configuration.'),
    R(7, 'AI SHOULD produce human-readable `name` and `slug` values.'),
    R(8, 'AI SHOULD treat later guide sections as reference manuals subordinate to this Contract.'),
    '',
    '### Artifact Envelope',
    '',
    R(9, 'Every artifact MUST include `schema_version`.'),
    R(10, '`schema_version` MUST equal `"1.0"` for artifacts authored against this guide.'),
    R(11, 'Every artifact MUST include `artifact_type` matching the payload (`indicator` | `screener` | `strategy`).'),
    R(12, 'Artifact body fields MUST use the portable key `definition` (not database column names such as `definition_json` or `config_json`).'),
    R(13, 'Every artifact MUST include `slug`.'),
    R(14, 'Every artifact MUST include `name`.'),
    R(15, 'Every artifact MUST include `metadata` as an object (empty object allowed).'),
    R(16, 'Unknown top-level envelope fields SHOULD NOT be introduced.'),
    R(17, '`minimum_engine_version` MAY be included; it is exported for documentation and is not currently enforced on Validate/Import. See Runtime Semantics.'),
    R(18, 'AI MUST NOT place Screener condition trees or Strategy scoring rows outside `definition`.'),
    '',
    '### Indicator Rules',
    '',
    R(19, 'Screeners MUST use only screenable Primary indicator ids from the Indicator Registry.'),
    R(20, 'Strategies MUST use only strategy-scorable Composite keys in `scoring_model`.'),
    R(21, 'No indicator id MUST be treated as both screenable and strategy-scorable.'),
    R(22, 'Indicator ids MUST exactly match the Indicator Registry (canonical spelling).'),
    R(23, 'Parameter names MUST exactly match the Indicator Registry for that indicator.'),
    R(24, 'Unknown indicator ids MUST NOT be invented.'),
    R(25, 'Unknown parameter names MUST NOT be invented.'),
    R(26, 'Parameter values that are present MUST be numeric where the registry requires numbers.'),
    R(27, 'Parameter values that are present MUST lie within catalogue min/max for that parameter.'),
    R(28, 'Out-of-range parameter values MUST NOT be authored (Validate rejects; values are not clamped).'),
    R(29, 'Screener `params` keys MAY be omitted; omitted keys resolve via TechnicalIndicatorService fallbacks. See Runtime Semantics / Indicator Registry.'),
    R(30, 'AI SHOULD set Screener `params` explicitly so runtime values match author intent (catalogue UI defaults may differ from runtime fallbacks).'),
    R(31, 'AI SHOULD NOT invent Indicator Registry shipping calculators; Indicator create/update drafts do not mutate release calculators.'),
    '',
    '### Screener Rules',
    '',
    R(32, 'Every Screener MUST contain `definition.root`.'),
    R(33, '`root` MUST be a `group` or `condition` node.'),
    R(34, 'Every Screener MUST contain at least one `condition` somewhere under `root`.'),
    R(35, 'Group operators MUST be exactly `AND` or `OR`.'),
    R(36, 'Condition operators MUST be exactly one of: `gt`, `gte`, `lt`, `lte`, `eq`.'),
    R(37, 'Unsupported operators MUST NOT be used (`neq`, `NOT`, `crosses_above`, `crosses_below`, `between`, `outside`, `contains`, `in`, and similar).'),
    R(38, 'Each of `left` and `right` MUST be either an indicator operand or a constant operand.'),
    R(39, 'Constant operands MUST use `{ "type": "constant", "value": <number> }`.'),
    R(40, 'Constant values MUST be numeric.'),
    R(41, 'Boolean operands MUST NOT be used.'),
    R(42, 'String operands MUST NOT be used.'),
    R(43, 'Null and date operands MUST NOT be used.'),
    R(44, 'Indicator operands MUST use `{ "indicator": "<id>", "params": { ... } }` shape (params object MAY be empty or omit keys per rules 29–30).'),
    R(45, 'Nesting depth MUST NOT exceed 4.'),
    R(46, 'Condition count MUST NOT exceed 40.'),
    R(47, '`eq` MUST be treated as float equality with documented epsilon; prefer `gte`/`lte` for thresholds. See Runtime Semantics.'),
    R(48, 'AI SHOULD express “between” as `AND` of `gte` and `lte`.'),
    R(49, 'AI SHOULD express “outside” as `OR` of `lt` and `gt`.'),
    R(50, 'Screener `slug` MUST be unique within the target portfolio (Import may suffix on collision).'),
    '',
    '### Strategy Rules',
    '',
    R(51, 'Strategies MUST reference Screeners via `screener_slug` and/or `screener_factory_key` in `eligibility_sources`.'),
    R(52, 'Strategies MUST NOT embed Screener trees (`definition.root`, nested condition groups) inside Strategy JSON. See Strategy Registry.'),
    R(53, '`definition.scoring_model` MUST be present (alias `indicators` is accepted and normalised).'),
    R(54, '`scoring_model` MUST contain at least one enabled row with `weight > 0`.'),
    R(55, 'Enabled scoring weights MUST total exactly 100 (±0.01).'),
    R(56, 'Only strategy-scorable Composite keys MAY appear as scoring `key` values.'),
    R(57, 'Unknown scoring keys MUST NOT be used.'),
    R(58, 'Scoring row `enabled: false` MUST be treated as excluded from weight totals and contributions. See Runtime Semantics.'),
    R(59, 'When both `minimum` and `maximum` are set on a scoring row, the pass band MUST be interpreted as `minimum <= value <= maximum`.'),
    R(60, 'Failing a scoring `minimum` or `maximum` MUST NOT reject the stock from Recommendations; contribution is zero and weight still dilutes overall. See Runtime Semantics.'),
    R(61, 'Multiple `eligibility_sources` MUST be interpreted as UNION of candidates (not intersection).'),
    R(62, '`priority` on eligibility sources MUST be treated as sort order only.'),
    R(63, 'Optional sections `thresholds`, `portfolio_rules`, `exit_strategy`, and `market_gates` MAY be included when documented in Strategy Registry.'),
    R(64, 'AI MUST NOT invent undocumented Strategy definition sections or exit rule keys.'),
    R(65, 'Exit rule keys MUST be chosen from the documented set (`ma_breakdown`, `rs_weakening`, `trend_weakening`, `score_exit`, `max_loss`, `atr_stop`, `trailing_stop`, `screener_exit`).'),
    R(66, '`exit_strategy.mode` MUST be `any` or `all` when present.'),
    R(67, 'AI SHOULD Export an existing Strategy before large edits to preserve thresholds, exits, and gates.'),
    '',
    '### Runtime Behaviour Rules (Authoring-Relevant)',
    '',
    R(68, 'AI MUST NOT assume failing one scoring factor hard-rejects the candidate. See Runtime Semantics §1.'),
    R(69, 'AI MUST NOT assume eligibility INTERSECTION when multiple Screeners are listed. See Runtime Semantics §3.'),
    R(70, 'AI MUST NOT assume `minimum_overall_score` is a hard Recommendation pipeline gate. See Runtime Semantics §4.'),
    R(71, 'AI SHOULD assume market gates demote OPEN/INCREASE only and still allow EXIT/REDUCE. See Runtime Semantics §6.'),
    R(72, 'AI SHOULD assume insufficient cash can demote OPEN/INCREASE to WATCH. See Runtime Semantics §7.'),
    R(73, 'AI MUST NOT embed exit logic as Screener trees inside Strategy; use documented exit rule keys or `screener_exit` references.'),
    '',
    '### Import Rules',
    '',
    R(74, 'JSON MUST be Validated successfully before Import.'),
    R(75, 'Import MUST NOT be performed on invalid or unvalidated JSON.'),
    R(76, 'AI SHOULD Validate after every material edit.'),
    R(77, 'AI SHOULD Import Screener before Strategy when the Strategy references that Screener by slug.'),
    R(78, 'Strategy Import MUST be treated as creating a draft until Select activates it.'),
    R(79, 'AI SHOULD prefer Export → edit → Validate → Import over authoring large optional sections from scratch.'),
    R(80, 'AI SHOULD expect Import normalisation (aliases, weight redistribution, slug/name collision handling). See Runtime Semantics §12.'),
    R(81, 'AI MUST NOT rely on unknown top-level Strategy definition keys surviving Import.'),
    '',
    '### Validation Rules',
    '',
    R(82, 'Validation errors MUST be resolved before Import.'),
    R(83, 'Invalid JSON MUST NOT be imported.'),
    R(84, 'Validation MUST precede every Import attempt.'),
    R(85, 'AI MUST treat Validate `ok` as the sole gate that enables Import in Registry UI.'),
    R(86, 'Editing JSON after Validate MUST be assumed to clear validation state.'),
    '',
    '### Portability Rules',
    '',
    R(87, 'JSON MUST remain portable across portfolios.'),
    R(88, 'JSON MUST NOT depend on local database primary keys for cross-portfolio portability.'),
    R(89, '`screener_slug` or `screener_factory_key` SHOULD be preferred over `screener_id` in authored Strategy eligibility.'),
    R(90, 'AI SHOULD declare `dependencies` when the envelope format includes them.'),
    R(91, 'AI MUST NOT hard-code portfolio-specific absolute paths or environment secrets into artifacts.'),
    '',
    '### AI Authoring Best Practices',
    '',
    R(92, 'AI SHOULD prefer canonical indicator and scoring ids.'),
    R(93, 'AI SHOULD NOT invent registry ids.'),
    R(94, 'AI SHOULD NOT invent operators.'),
    R(95, 'AI SHOULD NOT invent optional Strategy sections beyond those documented.'),
    R(96, 'AI SHOULD omit Screener parameters only when the intended runtime fallback is known and acceptable.'),
    R(97, 'AI SHOULD preserve existing metadata when editing an Exported artifact.'),
    R(98, 'AI SHOULD generate distinct, descriptive `name` strings.'),
    R(99, 'AI SHOULD use Trading Cookbook recipes when matching a documented investing style.'),
    R(100, 'AI SHOULD note Cookbook approximations (for example Value without PE, Darvas/CANSLIM proxies) in `metadata` or names when relevant.'),
    R(101, 'AI SHOULD keep one atomic concern per Screener where practical.'),
    R(102, 'AI SHOULD keep Strategy scoring rows aligned to Evaluation composites the engine actually emits.'),
    '',
    '### Pre-Generation Checklist',
    '',
    'Before emitting JSON, AI MUST verify:',
    '',
    '- [ ] `schema_version` present and equal to `"1.0"`',
    '- [ ] `artifact_type` correct (`screener` or `strategy`)',
    '- [ ] `slug`, `name`, `metadata`, and `definition` present',
    '- [ ] Field name is `definition` (not DB column names)',
    '- [ ] Indicator ids exist in Indicator Registry for the intended use (screenable vs scorable)',
    '- [ ] Parameter names match the registry; present values within min/max',
    '- [ ] Screener operators are only `gt`/`gte`/`lt`/`lte`/`eq` and groups only `AND`/`OR`',
    '- [ ] Operands are indicator or numeric constant only',
    '- [ ] Nesting depth ≤ 4 and condition count ≤ 40',
    '- [ ] Strategy has no embedded Screener trees',
    '- [ ] Eligibility uses `screener_slug` / `screener_factory_key`',
    '- [ ] Enabled `scoring_model` weights total exactly 100',
    '- [ ] Scoring keys are strategy-scorable only',
    '- [ ] Optional sections/keys are documented only',
    '- [ ] JSON is portable (no reliance on local DB ids)',
    '- [ ] Validate → Import sequence planned (Strategy remains draft until Select)',
    '',
].join('\n');

export const AI_AUTHORING_CONTRACT_TOPIC = {
    id: 'ai-authoring-contract',
    keyword: 'ai-authoring-contract',
    aliases: ['authoring-contract', 'normative-rules', 'artifact-contract'],
    title: 'AI Authoring Contract (Normative Rules)',
    routeLabel: '/docs/ai-authoring-contract.html',
    match: () => false,
    summary:
        'RFC 2119 constitution for authoring portable Trading Artifacts. Read this first; other guide sections are detailed reference.',
    overview: AI_AUTHORING_CONTRACT_MARKDOWN.replace(/^# AI Authoring Contract \(Normative Rules\)\n\n/, ''),
    controls: [
        {
            name: 'Download AI guide',
            description:
                'This Contract is the second major section of /docs/stox-trading-artifacts-ai-guide.md (after Introduction).',
        },
    ],
    concepts: [
        {
            name: 'Normative keywords',
            description: 'MUST / MUST NOT / SHOULD / SHOULD NOT / MAY per RFC 2119.',
        },
        {
            name: 'Contract-first maintenance',
            description:
                'New authoring or behavioural constraints are added to this Contract before detailed reference topics.',
        },
    ],
    related: [
        'authoring-trading-artifacts',
        'trading-artifact-runtime',
        'indicator-registry',
        'screener-registry',
        'strategy-registry',
        'trading-cookbook',
    ],
};
