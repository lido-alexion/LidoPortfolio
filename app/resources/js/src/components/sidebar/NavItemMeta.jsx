import React from 'react';
import NavBadge from './NavBadge';

/** @deprecated Use NavBadge. Kept for residual imports. */
export default function NavItemMeta(props) {
    return <NavBadge {...props} />;
}
